spring:
  application:
    name: nunes_sports
  datasource:
    url: jdbc:postgresql://localhost:5432/nunes_sports
    username: postgres
    password: arroz123
    driver-class-name: org.postgresql.Driver
    flyway:
      url: jdbc:postgresql://localhost:5432/nunes_sports
      user: postgres
      password: arroz123
      baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: update  # ou 'create'/'create-drop', dependendo do que você deseja
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
